const getCountOfValidPass = (input: string): number => {
  console.log(input)

  return 0
}

/*
byr (Birth Year)
iyr (Issue Year)
eyr (Expiration Year)
hgt (Height)
hcl (Hair Color)
ecl (Eye Color)
pid (Passport ID)
cid (Country ID)
*/

// only cid missing is fine
//

export default getCountOfValidPass
